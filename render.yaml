services:
  - type: web
    name: skintruth-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_final.py
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
